VITE_API_URL=http://localhost:3000

# Payment gateway UI — keep false in MVP; set true only in dev/staging with sandbox driver
VITE_GATEWAY_CHECKOUT_ENABLED=false

# Error tracking — omit to disable in local dev (CLAUDE.md Observability rules)
VITE_SENTRY_DSN=
