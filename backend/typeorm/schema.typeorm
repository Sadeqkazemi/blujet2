// This is your TypeORM schema file,
// learn more about it in the docs: https://pris.ly/d/typeorm-schema

// Get a free hosted Postgres database in seconds: `npx create-db`

generator client {
  provider = "typeorm-client"
  output   = "../generated/typeorm"
}

datasource db {
  provider = "postgresql"
}
